name: Runner Smoke Test

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - ".github/workflows/runner-smoke.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner started
        run: echo "RUNNER_OK"
