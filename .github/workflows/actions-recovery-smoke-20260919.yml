name: Actions Recovery Smoke 2026-09-19

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - ".github/workflows/actions-recovery-smoke-20260919.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Start runner
        run: |
          echo "RUNNER_STARTED"
          uname -a
